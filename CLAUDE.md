# Precificador Ohra

PWA de precificação/produção pra produtos impressos em 3D (Gustavo vende via Shopee, Mercado Livre, TikTok Shop, Shein). React 19 + Vite, Supabase (anon-key, RLS permissiva), deploy automático no Vercel a partir do `main` (`https://ohraa.vercel.app`).

## Regras que NUNCA podem ser esquecidas

- **Dado cadastrado nunca pode sumir quando o app muda.** Toda vez que uma mudança reestrutura onde/como um dado vive (nova tabela substituindo uma antiga, campo que muda de lugar, cálculo que passa a vir de outro lugar), a tarefa só está completa com uma migração SQL que carrega os dados que já existiam pra estrutura nova — nunca deixe alguém com produto/preço/canal já cadastrado "sumindo" da tela depois de uma melhoria. Migração deve ser aditiva e best-effort (usar `on conflict do nothing`/`do update`, nunca apagar a tabela antiga sem que Gustavo peça, e nunca travar se uma linha não achar par — só pula ela).
- **Gustavo mantém dados reais cadastrados o tempo todo** ("estou cadastrando algumas coisas, não apague"). Nunca criar/editar/excluir registros reais via ferramentas de automação/browser — verificação de deploy é sempre só leitura (comparar hash do JS buildado com o servido em produção, olhar console, print de tela). Ações destrutivas em dado real só acontecem via SQL que o próprio Gustavo roda no Supabase, nunca direto por mim.
- Eu não tenho acesso direto ao banco (sem MCP/DDL). Todo `ALTER TABLE`/migração vira um arquivo em `supabase/schema_vN.sql` (idempotente, sempre incrementando o número), colado no chat pra ele copiar e rodar no SQL Editor do Supabase — nunca só como arquivo anexado (ele prefere código colado direto na mensagem).
- Depois de qualquer alteração: `npm run lint` e `npm run build` (checar que não subiu warning novo além do baseline conhecido), commit com `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` + `Claude-Session: <url>`, push, esperar o deploy (~45s) e verificar via browser (comparar hash de `dist/assets/index-*.js` local com o servido, checar console sem erro).
- Gustavo prefere discutir/alinhar antes de mudanças maiores ("me retorne antes de alterar qualquer coisa") — pra ajustes pequenos e bem definidos pode implementar direto.

## Tabelas principais (Supabase)

`lojas`, `canais` (tipo: shopee/ml/tiktok/shein/custom — toda loja nova já nasce com shopee/ml/shein), `produtos_cadastro` (ganhou `material_id` no schema v22, vínculo por id com `materiais` — `material_nome` continua existindo como texto/fallback pra linhas sem vínculo confirmado), `kits` (+ `kit_produtos`, `kit_embalagens`; ganhou `markup_desejado`/`preco_sugerido` no schema v23, persistindo o que antes era só estado local da tela), `embalagens` (`unidade` virou seleção fixa — un/kg/g/m/cm/par/rolo — na tela desde o schema v21, que também normalizou valores antigos divergentes), `materiais`, `precos_canal` (preço real por produto/kit × canal, chave única `item_tipo,item_id,canal_id` — alimentada pelo "Salvar" da Precificação por Canal, lida pela grade "Produtos precificados", renomeada de "Preços por Canal"), `orcamentos_avulsos` (lista de encomendas avulsas salvas, com `quantidade` desde o schema v18 pra lotes de "Encomenda em volume"), `promocoes_salvas` (lista de promoções salvas em Promoções → Simular promoção, schema v17; ganhou `preco_referencia` e `desconto_pct` no schema v20, usados pra mostrar a coluna "Preço de" e pro modo simplificado de edição de promoções do tipo desconto; o tipo "brinde" foi retirado da tela de simulação, mas linhas antigas desse tipo continuam aparecendo normalmente em Promoções salvas), `metas_mensais` (meta de faturamento/lucro por mês — uma linha por `loja_id`+`mes` ('YYYY-MM') — usada na aba Metas, schema v19), `registros_impressao` (histórico de impressões — sucesso/falha, quantidade no lote, % em que falhou, custo do lote congelado no momento — schema v24, usado pela aba Registro de Impressões pra taxa de falha real e simulador de lote). A tabela antiga `produtos` (histórico solto, sem FK) foi descontinuada a partir do schema v10 — mantida no banco só como fonte de migração, não é mais lida pelo app.

## Navegação (reorganizada — ver histórico do schema v21-v23)

Grupos do menu (ordem do redesign "premium" de set/2026): Precificar (Custo de Produção, Registro de Impressões, Precificação por Canal, Comparativo), Catálogo (Produtos precificados, Cadastros), Vender (Orçamento, Promoções), Gestão (Metas, Ranking por Retorno, Otimização), Configuração ("Lojas, canais e taxas" — uma aba só, `Configuracao.jsx`, com sub-abas Lojas/Canais/Taxas Marketplace; chaves antigas "lojas"/"canais"/"taxas" salvas no localStorage ou passadas pra `irPara` são redirecionadas pra `config` + sub-aba); Tutorial fica no rodapé da barra lateral, junto do botão de tema. Grupos/rótulos/subtítulos ficam em `GRUPOS` no `App.jsx`; ícones de traço em `components/Icone.jsx` (chave = key da aba). A grade de preços salvos (`Historico.jsx`) chama-se "Produtos precificados".

## Registro de Impressões (schema v24)

Cada registro é UM job de impressão (uma "chapa"/lote) — se falha, o lote inteiro é considerado perdido (confirmado com o Gustavo: quando dá errado, geralmente perde tudo daquela impressão junto). A taxa de falha é GLOBAL por loja (não por produto — pouco dado ainda pra confiar numa taxa por produto; pode virar melhoria futura). Fórmula em `src/lib/registroImpressao.js`: taxa começa num prior conservador de 12,5% com peso equivalente a 15 registros (blend bayesiano), deslizando pra taxa real conforme mais registros entram. "Lote máximo seguro" não usa o prejuízo médio esperado (ele é matematicamente uma fração constante do custo, não varia por tamanho de lote) — usa o prejuízo SE FALHAR ("essa impressão específica deu errado") comparado a um teto em R$ que o Gustavo define, porque o que importa pra uma impressora rodando sozinha (ex: durante a noite) é quanto se perde de uma vez, não a média diluída ao longo de muitas impressões.

## Design system (redesign "premium", set/2026)

Pensado pra PC (conteúdo usa a largura toda até 1760px, espaçamento curto, tabelas largas), sem quebrar no celular (menu vira gaveta, grades viram 1 coluna, tabelas rolam de lado com `white-space: nowrap`). Tudo em `src/index.css`, tokens no `:root` (+ `[data-theme="dark"]`). Padrões: barra lateral escura + cabeçalho fixo (`.topbar`, título/subtítulo vêm de `GRUPOS`); `.panel` com o primeiro `h3` virando faixa de cabeçalho; `.panel > .table-wrap` cola a tabela nas bordas do cartão; `<Kpis itens=[...]>` (components/Kpis.jsx) é a linha de indicadores do topo (usada em Custo de Produção, Registro de Impressões, Precificação por Canal e Produtos precificados); `.subabas` = controle segmentado das sub-abas; `.toolbar` = busca + filtros acima de tabela; `.acoes-linha` = botões de ação que só aparecem no hover da linha; `<TopbarAcoes aba="...">` (portal) coloca os botões principais da tela (Salvar, Limpar, Novo, Exportar) no cabeçalho fixo — classe `so-pc` esconde um botão no celular; `.grid-auto` = grade de campos que se ajusta à largura; `.grid-cards` = cartões lado a lado; `h3` no meio de um `.panel` vira sub-seção com linha em cima. Precificação por Canal tem a tabela "Mesmo produto nos outros canais" (mesma conta do Comparativo); Produtos precificados exporta CSV (";" e vírgula decimal). Textos longos de explicação vão pro `<Ajuda>` (?) em vez de `.hint` solto na tela.
